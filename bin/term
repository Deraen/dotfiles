#!/bin/bash

if [[ $1 == "--help" ]]; then
  echo "Usage: $0 [number of terminals to start] [ARGS TO URXVT]"
  exit
fi

n=1
if [ "$1" -eq "$1" ] 2>/dev/null; then
  n=$1
  shift
fi

for i in $(seq 1 $n); do
  termite $@ &> /dev/null &
done
