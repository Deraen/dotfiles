#!/bin/bash

swaymsg -t get_tree | jq -e ".. | select(.instance? == \"keepassxc\" or .app_id == \"org.keepassxc.KeePassXC\")" | exec keepassxc
if [[ -n $SWAYSOCK ]]; then
    swaymsg -q "[app_id=org.keepassxc.KeePassXC] scratchpad show"
else
    swaymsg -q "[instance=keepassxc] scratchpad show"
fi
